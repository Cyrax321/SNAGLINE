"""Optional ``ml`` extra: echo-state-network ensemble (issue #80).

This subpackage is never imported by core code paths unless
``Config.ml_ensemble_enabled`` is set; ``import snagline`` works with
nothing but the standard library (project.md section 1.1).
"""
